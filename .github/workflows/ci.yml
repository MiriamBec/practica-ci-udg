name: Mi Primera Integración Continua (CI)

# "Ejecútate cada vez que haga un cambio (push)"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Paso 1: Descargar el código
      - uses: actions/checkout@v3
      
      # Paso 2: Imprimir un mensaje de saludo (Prueba)
      - name: Ejecutar script de prueba
        run: echo "¡Hola! La integración continua con GitHub Actions funcionó correctamente."
        
      # Paso 3: Simular una prueba exitosa
      - name: Verificación final
        run: echo "Todo está en orden."
